# Predicta · RAG ingestion (offline) — Python deps
pdfplumber>=0.11.0      # table extraction (tables are first-class here)
PyMuPDF>=1.24.0         # fast text + layout/positions (imported as `fitz`)
openai>=1.40.0          # embeddings (text-embedding-3-large)
supabase>=2.7.0         # upsert into Postgres/pgvector
tiktoken>=0.7.0         # token-aware chunking
python-dotenv>=1.0.1
tqdm>=4.66.0
